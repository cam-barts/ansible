.claude/worktrees/

# Ansible vault password — never commit. Use `vault_password_file` in
# ansible.cfg to point at one of these locally.
.vault_pass
*.vault_pass
.vault-password

# ansible-lint installs collections + caches here when run from the repo root
.ansible/
