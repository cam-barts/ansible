version: '3.3'
services:
    dashy:
        ports:
            - '4000:8080'
        volumes:
            - './conf.yml:/app/user-data/conf.yml'
        container_name: dashy
        restart: unless-stopped
        image: 'lissy93/dashy:latest'
