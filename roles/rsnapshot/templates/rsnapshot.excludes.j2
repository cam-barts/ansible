{{ ansible_managed | comment }}
.venv/
/home/home/
/home/nux/home/
# Database data directories: pirate user can't read mysql:mysql /
# postgres:postgres files anyway, and live-file backups of DBs are
# unreliable. Run dump hooks separately if you need DB backups.
**/vw-data/
**/wishthis/data/
**/wakapi/data/
**/vikunja/db/
**/f2b/data/db/
**/grav/config/log/
**/*.sqlite3-shm
**/*.sqlite3-wal
# More DB / cert / cache patterns discovered in the 2026-05-14 21:18 run:
# MongoDB data, semaphore postgres, Let's Encrypt account credentials,
# Arch AUR build cache (root-owned mid-build), and ruff's lint cache.
**/mongodb/
**/semaphore-postgres/
**/letsencrypt/accounts/
**/.cache/paru/
**/.ruff_cache/
# warrig media-automation stack (2026-08-24): torrents + arr-managed media
# under ~/data are bulk replaceable content, deliberately not backed up.
# rsnapshot passes --relative, so exclude anchoring is against / and paths
# arrive as /home/nux/data/... Patterns must carry the full path (same as
# the /home/nux/home/ entry above). Bare /data/... never matches. Only warrig
# has these; citadel has no ~/data.
/home/nux/data/torrents/
/home/nux/data/media/
